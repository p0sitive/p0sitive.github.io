<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>http://localhost:4000</id>
    <title>p0sitive的博客</title>
    <updated>2019-10-13T08:36:34.932Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="http://localhost:4000"/>
    <link rel="self" href="http://localhost:4000/atom.xml"/>
    <subtitle>温故而知新</subtitle>
    <logo>http://localhost:4000/images/avatar.png</logo>
    <icon>http://localhost:4000/favicon.ico</icon>
    <rights>All rights reserved 2019, p0sitive的博客</rights>
    <entry>
        <title type="html"><![CDATA[SVGA动画框架介绍]]></title>
        <id>http://localhost:4000/post/introduce-svga</id>
        <link href="http://localhost:4000/post/introduce-svga">
        </link>
        <updated>2019-10-13T08:10:14.000Z</updated>
        <summary type="html"><![CDATA[<p>人都是视觉动物，一个App的动效做的炫酷，优雅，是很吸引用户使用的。</p>
]]></summary>
        <content type="html"><![CDATA[<p>人都是视觉动物，一个App的动效做的炫酷，优雅，是很吸引用户使用的。</p>
<!-- more -->
<p>Android实现复杂动效的方式有如下几种：</p>
<ul>
<li>原生补间动画和属性动画实现</li>
<li>自定义View draw canvas实现</li>
<li>序列帧，使用帧动画显示，apng</li>
<li>GIF动图实现</li>
<li>Lottie，svga</li>
<li>显示视频</li>
<li></li>
</ul>
<p><strong>自定义View</strong>：需要自己实现动画的细节，开发效率和效果可能不够好。</p>
<p><strong>APNG</strong>：一整个png的序列图。png是支持alpha通道的，因此显示效果和以png为主的帧动画一致。但android原生ImageView并不支持这种图片格式的显示，需要第三方库去支持。</p>
<p><strong>Lottie</strong>：<br>
Lottie是Airbnb推出的开源跨平台动画库，通过AE做出动效导出json文件，然后移动端使用播放器进行原生渲染播放。<br>
<img src="https://pic4.zhimg.com/80/v2-0e4e99fb36b64d2241966fa96deadb51_hd.jpg" alt="image"></p>
<p><strong>SVGA</strong>:<br>
SVGA是YY直播公司开源的跨平台动效播放方案，和Lottie有着很相似的功能。</p>
<p><strong>svga和svg关系？</strong></p>
<p><strong>svg</strong>：</p>
<blockquote>
<p>Scalable Vector Graphics 可缩放矢量图形，是一个基于XML语言，描述二维矢量图形的图形格式。<br>
svg是由w3c制定，是一个开放标准。常见的图片格式如png，jpg，gif等都是位图，位图是由点构成的，矢量图则是由一些形状元素构成。该图中显示放大位图可以看到点，而放大矢量图看到的仍然是形状。SVG属于矢量图，因此能够无级缩放，而不会导致马赛克。</p>
</blockquote>
<p>svga是描述动效的文件，由Adobe After Effects导出动效，内部包含矢量图像，音效等，在1.x格式时使用ZIP进行打包，在2.x格式中，直接使用ProtoBuf+Zlib方式打包。因此svga文件不可直接打开预览。当然我们做为移动端开发人员不用关心svga内部文件结构实现。</p>
<blockquote>
<p>protocol buffers：<br>
Protocol buffers 是一种语言中立，平台无关，可扩展的序列化数据的格式，可用于通信协议，数据存储等。Protocol buffers 在序列化数据方面，它是灵活的，高效的。相比于 XML 来说，Protocol buffers 更加小巧，更加快速，更加简单。一旦定义了要处理的数据的数据结构之后，就可以利用 Protocol buffers 的代码生成工具生成相关的代码。甚至可以在无需重新部署程序的情况下更新数据结构。只需使用 Protobuf 对数据结构进行一次描述，即可利用各种不同语言或从各种不同数据流中对你的结构化数据轻松读写。<br>
Protocol buffers 很适合做数据存储或 RPC 数据交换格式。可用于通讯协议、数据存储等领域的语言无关、平台无关、可扩展的序列化结构数据格式。</p>
</blockquote>
<p>下面开始着重介绍一下svga框架。</p>
<figure data-type="image" tabindex="1"><img src="https://i.loli.net/2019/10/12/1ZX6unyNvMbAxhE.png" alt="image"></figure>
<p>上图是svga.io官网上的介绍图。从图中我们可知，svga格式动效支持Android、iOS、web。设计师在After Effects完成动效设计，使用SVGA Converter工具将动效资源转成svga文件。客户端使用SVGA-Parser将动效生成相应的player进行播放。</p>
<p>下面我们针对Android平台源码进行分析。</p>
<p>SVGA动画播放示例<br>
显示一个asset中svga动效流程如下：</p>
<pre><code>//显示svga动效 demo
private void loadAnimation() {
    //创建Parser
    SVGAParser parser = new SVGAParser(this);
    //从Asset文件夹中加载svga文件
    parser.decodeFromAssets(&quot;test.svga&quot;, new SVGAParser.ParseCompletion() {
        @Override
        public void onComplete(@NotNull SVGAVideoEntity videoItem) {
            //将转化后的VideoEntity设置给SVGAImageView
            animationView.setVideoItem(videoItem);
            //开启动效
            animationView.stepToFrame(0, true);
        }
        @Override
        public void onError() {

        }
    });
}
</code></pre>
<h3 id="svga动画文件解析">SVGA动画文件解析</h3>
<p><strong>SVGAParser</strong></p>
<p><code>SVGAParser</code>类是将svga文件转化成<code>SVGAImageView</code>识别的<code>VideoEntity</code>的转化类。根据源码可知，它支持三种方式加载svga文件。</p>
<pre><code>decodeFromAssets(string, callback)
decodeFromURL(string,callback)
decodeFromInputStream(inputStream,callback)
</code></pre>
<figure data-type="image" tabindex="2"><img src="https://i.loli.net/2019/10/12/UyGi9ThVq7XtvcK.png" alt="image.png"></figure>
<p>最终都会将已有的svga以<code>InputStream</code>方式加载，我们看一下<code>decodeFromInputStream</code>代码</p>
<pre><code>fun decodeFromInputStream(inputStream: InputStream, cacheKey: String, callback: ParseCompletion, closeInputStream: Boolean = false) {
    threadPoolExecutor.execute {
        try {
            readAsBytes(inputStream)?.let { bytes -&gt;
                //检查当前传入文件的是否有缓存
                if (bytes.size &gt; 4 &amp;&amp; bytes[0].toInt() == 80 &amp;&amp; bytes[1].toInt() == 75 &amp;&amp; bytes[2].toInt() == 3 &amp;&amp; bytes[3].toInt() == 4) {
                    //检查缓存文件
                    if (!buildCacheDir(cacheKey).exists()) {
                        ByteArrayInputStream(bytes).use {
                            //解压到该缓存目录下
                            unzip(it, cacheKey)
                        }
                    }
                    //从缓存中加载svga文件
                    this.decodeFromCacheKey(cacheKey, callback)
                }
                else {
                    inflate(bytes)?.let {
                        //使用SVGAVideoEntity类将传入的inputStream
                        val videoItem = SVGAVideoEntity(MovieEntity.ADAPTER.decode(it), File(cacheKey))
                        //将VideoEntity回调到主线程处理
                        videoItem.prepare {
                            this.invokeCompleteCallback(videoItem, callback)
                        }
                    }
                }
            }
        } catch (e: java.lang.Exception) {
            this.invokeErrorCallback(e, callback)
        } finally {
            if (closeInputStream) {
                inputStream.close()
            }
        }
    }
}
</code></pre>
<p><code>MovieEntity</code>是SVGA文件通过<code>protocol buffers</code>解释器解释后的产物。它内部包括了关键帧sprites、图片images、音频资源audios等信息。</p>
<figure data-type="image" tabindex="3"><img src="https://i.loli.net/2019/10/12/RpAILbSTC4HxasF.png" alt="debug-svga.png"></figure>
<p><code>SVGAVideoEntity</code>就是将<code>MovieEntity</code>的属性拿出来设置给自己，并通过传入的cacheKey文件添加缓存信息。</p>
<pre><code>class SVGAVideoEntity {
    ...
    var antiAlias = true
    var videoSize //动画显示大小
    var FPS = 15 //fps值
    var frames: Int = 0 //帧数
    //绘制帧
    internal var sprites: List&lt;SVGAVideoSpriteEntity&gt; = listOf()
    //音频
    internal var audios: List&lt;SVGAAudioEntity&gt; = listOf()
    //使用soundPool播放
    internal var soundPool: SoundPool? = null
    //图片
    internal var images = HashMap&lt;String, Bitmap&gt;()
    //缓存路径
    private var cacheDir: File

    constructor(obj: JSONObject, cacheDir: File) {
        this.cacheDir = cacheDir
        obj.optJSONObject(&quot;movie&quot;)?.let {
            it.optJSONObject(&quot;viewBox&quot;)?.let {
                videoSize = SVGARect(0.0, 0.0, it.optDouble(&quot;width&quot;, 0.0), it.optDouble(&quot;height&quot;, 0.0))
            }
            FPS = it.optInt(&quot;fps&quot;, 20)
            frames = it.optInt(&quot;frames&quot;, 0)
        }
        resetImages(obj)
        resetSprites(obj)
    }
    ...
    }

</code></pre>
<h3 id="svga动画播放">SVGA动画播放</h3>
<p>梳理完svga文件解析，下面介绍一下动画是如何播放的。<br>
svga动画的播放主要涉及到3个类：<code>SVGAImageView</code>、<code>SVGADrawable</code>、<code>SVGACanvasDrawer</code>。</p>
<p>如下图所示，<code>SVGAImageView</code>接收解析后的<code>VideoEntitiy</code>数据然后包装成<code>SVGADrawable</code>，通过<code>SVGADrawable</code>内部的<code>SVGADrawar</code>来绘制动画图形。</p>
<figure data-type="image" tabindex="4"><img src="https://i.loli.net/2019/10/12/agif9KBktpOdShl.png" alt="svga-imageview.png"></figure>
<p><code>SVGAImageView</code>是显示动画的类，继承自ImageView。主要关注两个方法：<code>setVideoItem</code>和<code>startAnimtion</code>。</p>
<pre><code>fun setVideoItem(videoItem: SVGAVideoEntity?, dynamicItem: SVGADynamicEntity?) {
    if (videoItem == null) {
        setImageDrawable(null)
        return
    }
    //将传入的VideoEntity和DynamicEntity包装成一个SVGADrawable
    val drawable = SVGADrawable(videoItem, dynamicItem ?: SVGADynamicEntity())
    drawable.cleared = clearsAfterStop
    //将此Drawable设置为imageView的Drawable
    setImageDrawable(drawable)
}


fun startAnimation(range: SVGARange?, reverse: Boolean = false) {
    stopAnimation(false)
    //当前的Drawable是SVGADrawable
    val drawable = drawable as? SVGADrawable ?: return
    //初始化Drawable的属性
    drawable.cleared = false
    drawable.scaleType = scaleType
    //VideoItem中记录着动画的帧数和显示内容
    drawable.videoItem.let {
        var durationScale = 1.0
        //确定起始帧
        val startFrame = Math.max(0, range?.location ?: 0)
        val endFrame = Math.min(it.frames - 1, ((range?.location ?: 0) + (range?.length ?: Int.MAX_VALUE) - 1))
        val animator = ValueAnimator.ofInt(startFrame, endFrame)
        //这个try-catch代码块主要是恢复在开发者选项中设置的动画缩放参数，防止在播放SVGA动画时出现帧数错误
        try {
            ...
        } catch (e: Exception) {}
        animator.interpolator = LinearInterpolator()
        //动画时长是由帧数和FPS决定的
        animator.duration = ((endFrame - startFrame + 1) * (1000 / it.FPS) / durationScale).toLong()
        animator.repeatCount = if (loops &lt;= 0) 99999 else loops - 1
        animator.addUpdateListener {
            //更新关键帧
            drawable.currentFrame = animator.animatedValue as Int
            callback?.onStep(drawable.currentFrame, ((drawable.currentFrame + 1).toDouble() / drawable.videoItem.frames.toDouble()))
        }
        ...忽略其他...
    }
}
</code></pre>
<p><code>SVGADrawable</code>内部有个关键的属性<code>currentFrame</code>，用来记录当前绘制的哪一帧，发生改变时调用自身的<code>invalidateSelf()</code>方法进行绘制。</p>
<pre><code>var currentFrame = 0
    internal set (value) {
        if (field == value) {
            return
        }
        field = value
        invalidateSelf()
    }
</code></pre>
<p><code>SVGACanvasDrawer</code>是最终的绘制类，主要调用<code>drawFrame</code>来绘制每个元素</p>
<pre><code>override fun drawFrame(canvas: Canvas, frameIndex: Int, scaleType: ImageView.ScaleType) {
    super.drawFrame(canvas,frameIndex, scaleType)
    this.pathCache.onSizeChanged(canvas)
    //获取当前要绘制的数据
    val sprites = requestFrameSprites(frameIndex)
    sprites.forEach {
        //绘制每一个元素
        drawSprite(it, canvas, frameIndex)
    }
    //播放音频
    playAudio(frameIndex)
}
</code></pre>
<p>流程基本梳理完成了，简单来说就是svga内部记录了所有要绘制的元素和每一帧信息，直接通过遍历当前帧进行绘制即可，效率比较高。</p>
<p>下面做一个svga和Lottie的对比</p>
<p>以Lottie官方给出的点赞动效为例，我下载了最新版<code>Adobe After Effects 2019</code>，并且安装了svga和Lottie导出动画的插件<code>BodyMovin</code>和<code>SGVA Converter</code>。分别导出这个动效的json文件和svga文件</p>
<p>并且放到一个简单页面中播放对比。两个文件都放置在asset文件夹中，播放页面中没有其他UI元素。</p>
<table>
<thead>
<tr>
<th>文件</th>
<th>文件大小</th>
<th>CPU</th>
<th>内存占用</th>
<th>GPU程式分析</th>
</tr>
</thead>
<tbody>
<tr>
<td>svga</td>
<td>11k</td>
<td>6%</td>
<td>108.8m</td>
<td>如下图：每一帧都在绿线以下</td>
</tr>
<tr>
<td>Lottie</td>
<td>35k</td>
<td>13%</td>
<td>121.2m</td>
<td>绿色和蓝色较长表示进行了过多的运算</td>
</tr>
</tbody>
</table>
<p>svga</p>
<figure data-type="image" tabindex="5"><img src="https://i.loli.net/2019/10/12/ljSDzRAkHPwmBxb.png" alt="svga-gpu.png"></figure>
<p>Lottie<br>
<img src="https://i.loli.net/2019/10/12/HfCbBl6qLuYtTwX.png" alt="lottie-gpu.png"></p>
<p>分析可知：<br>
Lottie文件是导出的json文件，内部的元素明文显示。svga则是使用了<code>protocol buffers</code>+<code>zlib</code>方式做了压缩，文件体积比较小。在播放动效时，svga在导出时已经做了高阶差值计算，因此只用绘制图形即可，相较于Lottie绘制效率较高。</p>
<p>参考：</p>
<ul>
<li>https://zh.wikipedia.org/wiki/%E5%8F%AF%E7%B8%AE%E6%94%BE%E5%90%91%E9%87%8F%E5%9C%96%E5%BD%A2</li>
<li>https://halfrost.com/protobuf_encode/</li>
<li>https://svga.io/intro.html</li>
<li>https://jfson.github.io/2018/01/08/41-anim/</li>
<li>https://jfson.github.io/2018/06/21/49-svga/</li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[ImageView ScaleType介绍]]></title>
        <id>http://localhost:4000/post/imageview-scale-type</id>
        <link href="http://localhost:4000/post/imageview-scale-type">
        </link>
        <updated>2016-10-13T08:20:35.000Z</updated>
        <content type="html"><![CDATA[<p>我们可以通过ImageView的ScaleType属性来实现图片在ImageView的展示方式。通过阅读源码可知ScaleType设置是通过ImageView中的Matrix变换得来的。在此做一个简单的文字记录</p>
<p>ImageView的scaleType的属性有好几种，分别是matrix（默认）、center、centerCrop、centerInside、fitCenter、fitEnd、fitStart、fitXY</p>
<p>基本使用：<br>
布局文件中<code>android:scaleType=&quot;xxxx&quot;</code><br>
代码中：<code>imageView.setScaleType(ImageView.ScaleType.FIT_XY)</code></p>
<p>下面主要介绍一下各个类型显示是怎么样的<br>
<code>android:scaleType=&quot;center&quot;</code></p>
<p>保持原图的大小，显示在ImageView的中心。当原图的size大于ImageView的size，超过部分裁剪处理。</p>
<p><code>android:scaleType=&quot;centerCrop&quot;</code></p>
<p>以填满整个ImageView为目的，将原图的中心对准ImageView的中心，等比例放大原图，直到填满ImageView为止（指的是ImageView的宽和高都要填满），原图超过ImageView的部分作裁剪处理。</p>
<p><code>android:scaleType=&quot;centerInside&quot;</code></p>
<p>以原图完全显示为目的，将图片的内容完整居中显示，通过按比例缩小原图的size宽(高)等于或小于ImageView的宽(高)。如果原图的size本身就小于ImageView的size，则原图的size不作任何处理，居中显示在ImageView。</p>
<p><code>android:scaleType=&quot;matrix&quot;</code></p>
<p>不改变原图的大小，从ImageView的左上角开始绘制原图，原图超过ImageView的部分作裁剪处理。</p>
<p><code>android:scaleType=&quot;fitCenter&quot;</code></p>
<p>把原图按比例扩大或缩小到ImageView的ImageView的高度，居中显示</p>
<p><code>android:scaleType=&quot;fitEnd&quot;</code></p>
<p>把原图按比例扩大(缩小)到ImageView的高度，显示在ImageView的下部分位置</p>
<p><code>android:scaleType=&quot;fitStart&quot;</code></p>
<p>把原图按比例扩大(缩小)到ImageView的高度，显示在ImageView的上部分位置</p>
<p><code>android:scaleType=&quot;fitXY&quot;</code></p>
<p>把原图按照指定的大小在View中显示，拉伸显示图片，不保持原比例，填满ImageView.</p>
<p>ImageView设置ScaleType部分源码</p>
<pre><code class="language-java">private void configureBounds() {
    if (mDrawable == null || !mHaveFrame) {
        return;
    }

    int dwidth = mDrawableWidth;
    int dheight = mDrawableHeight;

    int vwidth = getWidth() - mPaddingLeft - mPaddingRight;
    int vheight = getHeight() - mPaddingTop - mPaddingBottom;

    boolean fits = (dwidth &lt; 0 || vwidth == dwidth) &amp;&amp;
                   (dheight &lt; 0 || vheight == dheight);

    if (dwidth &lt;= 0 || dheight &lt;= 0 || ScaleType.FIT_XY == mScaleType) {
        /* 
            如果图片没有固定的大小，我们会使用fit方式，铺满整个view
        */
        mDrawable.setBounds(0, 0, vwidth, vheight);
        mDrawMatrix = null;
    } else {
        // 我们要设置相应的缩放类型，需要图片自己的大小
        mDrawable.setBounds(0, 0, dwidth, dheight);

        if (ScaleType.MATRIX == mScaleType) {
            // Use the specified matrix as-is.
            if (mMatrix.isIdentity()) {
                mDrawMatrix = null;
            } else {
                mDrawMatrix = mMatrix;
            }
        } else if (fits) {
            // 当前的bitmap是fit方式，没有必要做变换
            mDrawMatrix = null;
        } else if (ScaleType.CENTER == mScaleType) {
            // 居中展示bitmap，不需要缩放，仅仅是将图片和view的中心点对齐即可
            mDrawMatrix = mMatrix;
            mDrawMatrix.setTranslate(Math.round((vwidth - dwidth) * 0.5f),
                                     Math.round((vheight - dheight) * 0.5f));
        } else if (ScaleType.CENTER_CROP == mScaleType) {
            mDrawMatrix = mMatrix;
            //CenterCrop方式，先缩放，然后居中（会有dx或dy的移除效果）
            float scale;
            float dx = 0, dy = 0;

            if (dwidth * vheight &gt; vwidth * dheight) {
                scale = (float) vheight / (float) dheight; 
                dx = (vwidth - dwidth * scale) * 0.5f;
            } else {
                scale = (float) vwidth / (float) dwidth;
                dy = (vheight - dheight * scale) * 0.5f;
            }

            mDrawMatrix.setScale(scale, scale);
            mDrawMatrix.postTranslate(Math.round(dx), Math.round(dy));
        } else if (ScaleType.CENTER_INSIDE == mScaleType) {
            //先缩放，后居中，完全展示
            mDrawMatrix = mMatrix;
            float scale;
            float dx;
            float dy;
            
            if (dwidth &lt;= vwidth &amp;&amp; dheight &lt;= vheight) {
                scale = 1.0f;
            } else {
                scale = Math.min((float) vwidth / (float) dwidth,
                        (float) vheight / (float) dheight);
            }
            
            dx = Math.round((vwidth - dwidth * scale) * 0.5f);
            dy = Math.round((vheight - dheight * scale) * 0.5f);

            mDrawMatrix.setScale(scale, scale);
            mDrawMatrix.postTranslate(dx, dy);
        } else {
            // 针对主动设置变换
            mTempSrc.set(0, 0, dwidth, dheight);
            mTempDst.set(0, 0, vwidth, vheight);
            
            mDrawMatrix = mMatrix;
            mDrawMatrix.setRectToRect(mTempSrc, mTempDst, scaleTypeToScaleToFit(mScaleType));
        }
    }
}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[使用Gradle自动设置app的versionCode]]></title>
        <id>http://localhost:4000/post/use-gradle-to-make-app-versionCode</id>
        <link href="http://localhost:4000/post/use-gradle-to-make-app-versionCode">
        </link>
        <updated>2016-05-14T07:58:51.000Z</updated>
        <content type="html"><![CDATA[<h3 id="前言">前言</h3>
<p>在module的build.gradle文件中，记录着当前app的<code>versionCode</code>和<code>versionName</code>两个参数，用来标示app的版本信息。</p>
<blockquote>
<ul>
<li>versionCode：int型的值，大多数的app通过对比versionCode值，来提示用户是否需要更新。</li>
<li>versionName：指示当前版本信息，一般为“1.2”、“v2.0.3”等等，方便运维以及统计的SDK记录bug和反馈信息。</li>
</ul>
</blockquote>
<p>对于versionCode的值，每次release之前都要手动的自增修改，岂不是很low？下面介绍我了解的两种方式。</p>
<h2 id="versioncode自动化方式">versionCode自动化方式</h2>
<h3 id="使用git-commit">使用git commit</h3>
<p>若项目使用git做版本管理，那么可以使用项目的commit值作为<code>versionCode</code>，一般来说commit值不会大到漫无边际，5位到6位已经是很大了。另外<code>versionCode</code>是int类型的值，2^31你懂的，一个app的<code>versionCode</code>也不会大到如此地步。😅</p>
<p>闲话少说，开始吧</p>
<p>首先在build.gradle中定义如下变量</p>
<pre><code>def cmd ='git rev-list HEAD --count'
def getVersion = cmd.execute().text.trim().toInteger();
</code></pre>
<p>在android标签下的<code>defaultConfig</code>中替换<code>versionCode</code>为上面我们定义的<code>getVersion</code>变量即可。还是蛮简单的哈。</p>
<p>注意</p>
<p>变量定义在android标签之外<br>
git命令rev-list HEAD记录的是当前分支的总commit数，因此在打包时分支应切换为与git仓库中对应的分支</p>
<h3 id="使用文件">使用文件</h3>
<p>在项目的根目录创建<code>version.properties</code>文件，在文件中记录当前的版本数</p>
<pre><code>VERSION_CODE=1
</code></pre>
<p>然后在build.gradle文件中添加</p>
<pre><code>def versionFile = file('version.properties')

if (versionFile.canRead()) {
    def Properties versionProps = new Properties()

    versionProps.load(new FileInputStream(versionFile))

    def code = versionProps['VERSION_CODE'].toInteger() + 1

    versionProps['VERSION_CODE']=code.toString()
    versionProps.store(versionPropsFile.newWriter(), null)
}
else {
    throw new GradleException(&quot;Could not read version.properties!&quot;)
}
</code></pre>
<h3 id="修改编译后apk名称">修改编译后apk名称</h3>
<p>修改完<code>versionCode</code>之后，就想设置一下编译后文件名称，方便测试和运维使用。</p>
<p>一般不做修改的apk名称就是app-release.apk,仅仅是module名称+编译类型，一点都体现不出<code>versionName</code>以及<code>versionCode</code>信息。</p>
<p>我们可以通过修改build.gradle文件中android标签内的输出文件，来修改编译后apk文件名称。</p>
<pre><code>  android.applicationVariants.all{ variant -&gt;
    variant.outputs.each{ output-&gt;
        def outputFile = output.outputFile
        if(outputFile != null &amp;&amp; outputFile.name.endsWith('.apk')){
            def fileName = &quot;APP-${defaultConfig.versionName}_${defaultConfig.versionCode}}.apk&quot;
            output.outputFile = new File(outputFile.parent,fileName)
        }
    }
}
</code></pre>
<p>fileName修改成想要的就ok了，比如添加编译时间，发行渠道应用市场等等</p>
<pre><code>def getReleaseTime(){
return new Date().format(&quot;yyyy-MM-dd&quot;,TimeZone.getTimeZone(&quot;UTC&quot;));
}
</code></pre>
<p>参考</p>
<ol>
<li>http://my.oschina.net/mengshuai/blog/551356?fromerr=7WKXr2ee</li>
<li>http://www.race604.com/android-auto-version/</li>
</ol>
]]></content>
    </entry>
</feed>